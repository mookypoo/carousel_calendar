import 'package:flutter/cupertino.dart';

class IosMainPage extends StatelessWidget {
  const IosMainPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Container(),
    );
  }
}
