import 'package:flutter/material.dart';

class AndroidAds extends StatelessWidget {
  const AndroidAds({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
