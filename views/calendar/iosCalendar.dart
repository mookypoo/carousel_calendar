import 'package:flutter/cupertino.dart';

class IosCalendar extends StatelessWidget {
  const IosCalendar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
